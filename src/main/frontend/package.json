{
  "name": "tallerwebi-frontend",
  "version": "1.0.0",
  "description": "Frontend styles (CSS-only): Vite + Tailwind as build-time tools; all logic lives in Java",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "vite build",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "browser-sync": "^3.0.4",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2"
  }
}
